services:
  uno:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped

  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --url http://uno:8000 --no-autoupdate
    depends_on:
      - uno
    restart: unless-stopped
